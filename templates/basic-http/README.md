# Basic HTTP crawler
Fetches one page with `HttpCrawler` and prints its status and body length.
After generation, run `cargo run`.
See the [first HTTP crawler guide](https://github.com/satvik007/millipede/blob/main/docs/guide/getting-started.md#first-http-crawler).
